# Define a list of numbers
numbers = [10, 20, 30, 40, 50]
# Create an iterator object from the list
it = iter(numbers)
# Fetch the first element using next()
print(next(it))
# Fetch the second element using next()
print(next(it))
